<%= simple_form_for([:admin, member]) do |f| %>
  <%= f.input :full_name %>
  <%= f.input :joined_at, as: :date %>
  <%= f.input :email %>
  <%= f.input :address, as: :text, input_html: { rows: 3 } %>
  <%= f.button :submit %>
<% end %>

<%= link_to 'Cancel', [:admin, @member] %>
