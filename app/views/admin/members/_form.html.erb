<%= simple_form_for([:admin, member]) do |f| %>
  <%= f.input :full_name %>
  <%= f.input :joined_at, as: :date %>
  <%= f.input :email %>
  <%= f.input :phone %>
  <%= f.button :submit %>
<% end %>

<%= link_to t(:cancel), [:admin, @member] %>
