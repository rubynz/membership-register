<p id="notice"><%= notice %></p>

<h1>Members</h1>

<p>
  This is a legal document. The secretary of
  <%= MembershipRegister.society_name %>
  is required to maintain this membership register
  and can be asked to provide it  if the society is audited.

  More details can be found on the
  <%= link_to 'societies website', 'http://www.societies.govt.nz/cms/incorporated-societies/running-a-society/what-records-should-you-keep' %>
</p>

<table>
  <thead>
    <tr>
      <th>Full name</th>
      <th>Email</th>
      <th>Address</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @members.each do |member| %>
      <tr>
        <td><%= member.full_name %></td>
        <td><%= member.email %></td>
        <td><%= member.address %></td>
        <td><%= link_to 'Show', member %></td>
        <td><%= link_to 'Edit', edit_member_path(member) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Member', new_member_path %>
