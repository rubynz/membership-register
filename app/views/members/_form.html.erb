<%= simple_form_for(member) do |f| %>
  <%= f.input :full_name %>
  <%= f.input :email %>
  <%= f.input :address, as: :text, input_html: { rows: 3 } %>
  <%= f.button :submit %>
<% end %>

<%= link_to 'Cancel Changes', @member if @member.persisted? %>
